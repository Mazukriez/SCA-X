# Cognitive State Model

The fundamental unit is a Cognitive State Transition.

S(t) + E(t) + G + C → H → P → A → S(t+1)

A state contains semantic objects, relationships, constraints, observations, active goals, hypotheses, confidence and provenance. A transition records preconditions, action, predicted effects, actual effects and outcome.
