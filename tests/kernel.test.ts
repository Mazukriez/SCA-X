import {describe,expect,it} from 'vitest';import {buildCognition,semanticize} from '../core/kernel';
describe('SCA-X cognitive kernel',()=>{it('constructs cybersecurity state',()=>{const s=semanticize('Investigate a suspicious PowerShell execution on a critical server');expect(s.entities).toContain('PowerShell');expect(s.constraints).toContain('Preserve evidence')});it('generates hypotheses and plan',()=>{const r=buildCognition('Investigate a suspicious PowerShell execution on a critical server');expect(r.hypotheses).toHaveLength(3);expect(r.plan).toHaveLength(4)});it('supports migration',()=>{const r=buildCognition('Design a low-risk migration from AWS to GCP');expect(r.semanticState.entities).toContain('AWS');expect(r.experience.pattern).toMatch(/Phased/i)});});
